# 主编点评风格样本（few-shot 注入 generate-radar.mjs）

# 用法：
# - 每条样本一行，以 "- " 开头，写 50–100 字，就像你平时写 editor_note 那样
# - 建议 3–5 条；少于 1 条有效样本时脚本自动跳过注入
# - 以 # 开头的行是注释，不会被注入
# - 改完直接 commit 到 main，下一次雷达生成立即生效
#
# 写作参考（样本体现的口吻特征）：
# - 第一人称：我/我看/我不推荐
# - 有明确立场，敢下判断，不中立和稀泥
# - 落到"对一人创业者意味着什么"，不讲空话
#
# ─── 在下面替换为你自己写的样本 ───

# - 示例：这个案例我最看重的不是收入数字，而是它证明了"一个痛点+一个 wrapper"就能跑通现金流。独立开发者今晚就能抄作业。
# - 示例：大厂又发新模型了，热闹是他们的。对一人公司来说真正的新闻是 API 降价——成本结构直接改善，该重新算算你的单位经济了。
