name: Nitter Media Format Probe (TEMP)

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Dump real item descriptions with images
        run: python3 scripts/media-dump.py
